version: 3
jobs:
    build: 
      docker: 
        - image: node 
      steps:
        - checkout
        - run: 
          name: Init
          command: npm test 
      branches:
        only: 
        - gh-pages